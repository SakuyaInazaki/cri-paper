# AGENT.md — 通用审稿 skill（cri-paper）

本文件面向在这个仓库里工作的任何 agent 和人。先读这里，再读 `.agent/knowledge/README.md` 的索引，再读 `.agent/notes/` 里最新几条。

## 1. 项目是什么

这个仓库是一个**通用的审稿 Claude Code skill**，公开发布在 <https://github.com/SakuyaInazaki/cri-paper>，任何审稿人 `git clone` 到 `~/.claude/skills/cri-paper` 即装即用。入口是根目录的 `SKILL.md`。

它做的事是把「审稿人怎么用 AI 辅助、能用到什么程度」这件事固化成一条可照做的流程：先按会议政策定合规档位，再拆论文、核验、写 3–4 条带位置的意见、去 AI 味、按需披露。它不替用户下接收或拒稿结论。

它是从用户自己的审稿工作台改造来的。原来的调研产出留在 `survey/`（五份报告，00 是入口），设计规格留在 `docs/`，起源材料留在 `background/`。skill 里的每条政策措辞与每个数字都要能回指到 `survey/` 的某一节，遇到「这条从哪来的」就回去查。

用户实际开审时会出现稿件与审稿意见草稿，它们**不进仓库**，见第 4 节。

## 2. 目录

```
SKILL.md                     skill 入口：红线、三种模式、五步流程。按需加载 references/
README.md                    给人看的仓库简介（中英双语各一节）
AGENT.md                     本文件
.gitignore                   保密材料的兜底规则，顶部写明红线
references/                  按需加载的操作文件
  policy-tiers.md            三档合规政策速查（12 家机构逐行，含原文要点与链接）
  paper-decomposition.md     拆分操作手册（十二层）
  figure-checklist.md        图表逐张清单
  comment-shape.md           意见形状：3–4 条、每条一句、位置+问题+建议动作，附正反例
  de-ai-checklist.md         去 AI 味：中文调 human-writing 的方法，英文手动清单
  prompts.md                 可复制 prompt 集
templates/
  paper-card.md              空白论文卡（十二层，列结构按规格 §6 第 1 条）
scripts/
  pdf_skeleton.py            骨架生成（零网络零 LLM）
  numeric_consistency.py     第 10 层数值对账（零网络）
  check_refs.py              第 7 层引用核真（会外发引用指纹，受档位门控）
  README.md                  三脚本的用法与合规表
examples/                    公开论文上的骨架实测记录（2504.09737）
docs/
  skill-design-v1.md         skill 设计定稿
  paper-decomposition-spec-v1.md  拆分规格（含 §6 裁定）
background/                  项目起源材料（同事建议原文、human-writing 上游链接）
survey/
  00-总览与推荐工作流.md      入口。三档合规政策、同事建议与证据的对账、
                             每篇 20–30 分钟的流水线、本机资产缺口、可直接复制的审稿操作卡
  01-AI辅助审稿-学术实践与政策.md
                             学术实践与政策。已发表/开源的系统、各家会议与出版商的政策原文口径（§2 最关键）、
                             审稿人可操作步骤、AI 痕迹识别（文本/引用/图）
  02-审稿skill与工作流生态盘点.md
                             生态盘点。Claude Code / Codex / opencode 里的审稿 skill 与 agent、
                             经典 prompt 模板与 rubric、中文社区做法与踩坑、去 AI 味工具同类盘点、推荐清单
  03-本地资产-human-writing与ARS审稿skill.md
                             本机已装资产审计。human-writing 的禁令表与适用边界、
                             ARS academic-paper-reviewer 的模式选择与三处 rubric 硬伤、约束 prompt、遗留缺口
  04-论文拆分方法调研.md      自动审稿系统怎么拆论文、人类审稿方法论、主张证据对齐、
                             本地 PDF 结构化工具与引用核真的合规判断（§4.1）、图表清单、对九层草案的差分意见
.agent/
  README.md                  记录体系的规则（编号格式、notes 与 knowledge 的分工）
  notes/                     过程记录，每次实质变动一条，只增不改
  knowledge/                 当前认知，每条可被后续证据推翻，推翻要留痕
```

将来会出现、且已经在 `.gitignore` 里的保密目录：

- `manuscripts/`、`papers/`、`submissions/` — 待审的投稿稿件原文。
- `reviews/`、`drafts/` — 审稿意见草稿与中间稿。
- `scratch/` — 临时文件。

这些目录里的东西一律不进 git。`*.pdf`、`*.docx`、`*.doc`、`*.tex` 也按扩展名整体排除。

## 3. 工作约定

- **分工**：主会话只做需求澄清、方案拆解、任务分发、结果验收；实现类工作派 Opus 子 agent 做。主会话对子 agent 交回来的关键断言要逐条在线复核，不直接采信。
- **过程记录**：每次会话结束前在 `.agent/notes/` 追加一条记录，写清做了什么、为什么、还考虑过什么、没做什么。记录只增不改，纠正用新记录指向旧记录。
- **认知落盘**：稳定认知写进 `.agent/knowledge/`，每条带状态、置信度、来源。**knowledge 里的条目可以被后续证据推翻**，推翻时不删旧条目，改状态为「已推翻」并写明依据是什么、被哪一条取代。读旧条目先看状态，不把历史认知当金科玉律。
- **数字要有出处**：报告里的每个数字、每条政策措辞，都要能指回具体的报告章节或原始政策页。不确定就标不确定，不为凑数编。写 `references/` 下的文件时用「见 survey/01 §2」这种形式把出处写在文内。
- **改完要复跑**：改动 `SKILL.md` 或 `references/` 下任何文件之后，要在公开论文示例上把三个脚本复跑一遍，确认流程里引用的命令、路径、输出字段还对得上。实测记录写进 `examples/`，测试稿用 arXiv 2504.09737 这类公开论文，绝不用真实投稿。
- **语言**：文档与注释用中文，提交信息中英皆可。

## 4. 保密红线

这一节优先于本文件其他任何内容。

- **稿件原文不提交、不上传。** 待审稿件的 PDF、正文、图表，一律不进仓库，也不喂给非隐私合规的模型。
- **参考文献列表同样算稿件的一部分。** 引用核查虽然性价比最高，但第一档（全禁）的会议下连参考文献列表都不能上传，见 `survey/00` 第三部分第 2 步。
- **审稿意见草稿不提交、不上传到非隐私合规的模型。** ARR 明文写了连审稿人自己写的报告都不能传到非隐私保护的工具上。
- **开审前先查会议政策定档。** 这是第 0 步，两分钟，没定档之前不要开任何模型。三档的划分与代表会议见 `survey/00` 第一部分，政策原文口径见 `survey/01 §2`。
  - 第一档全禁（CVPR 2026、ECCV 2026、Elsevier、NIH，离线模型也点名禁了）。
  - 第二档允许把稿件喂给隐私合规模型做理解与润色，但不许让它评判（ICML 2026 Policy B）。
  - 第三档只许润色自己写的意见，并且要披露（Wiley、Springer Nature、ARR、ICLR 2026）。
- **违规是有后果的，而且今年已经可被机器检出。** ICML 2026 在投稿 PDF 里埋隐藏水印短语，查实约 795 条违规评审；ICLR 2026 与 CVPR 2026 的处罚落点都是把违规审稿人自己投的论文 desk reject。
- **不要跑 ARS 的 `calibration` 模式**，它默认开跨模型，会把稿件发给另一家 provider（`survey/03 §2.7`）。

## 5. 审稿意见的固定形状

最终交给会议的意见，形状是定死的，改稿时也不许被改掉：

- **3–4 条**，不多写。
- **每条一句话**。
- 每条的结构是**位置 + 问题 + 建议动作**。位置要具体到页码、节号、图号、表里的数字或公式编号。
- **不加粗、不用破折号、不用三项排比**，不写「值得注意的是」「总的来说」这类路标句，不给每条配冒号小标题。

为什么是这个形状：条数少不影响质量，含糊才影响；而带具体位置的句子只有真读过论文的人写得出来，这是目前唯一可靠的「去 AI 味」手段——五种主流检测器都会把「人写加 LLM 润色」的评审误判成 AI 生成，光改文风不足以自证清白。依据见 `survey/00` 第二部分、`survey/01 §1.1`、`§3`、`§5.4`。

中文意见改稿用本机已装的 `human-writing`（`~/.claude/skills/human-writing/`，须显式调用），调用 prompt 与机械体检命令在 `survey/00` 第五部分。英文本机没有任何去 AI 味资产，只能对照 `survey/00` 第五部分那份手动清单逐条自查。

## 6. 当前状态与悬而未决

状态：调研阶段完成（`survey/00`–`04`），设计定稿（`docs/` 两份规格），skill 主体已建成。`SKILL.md`、`references/` 六份、`templates/paper-card.md`、`scripts/` 三个脚本、`examples/` 实测记录都在位。仓库已转 public，起源材料归档进 `background/`。三个脚本在公开论文 arXiv 2504.09737 上跑通过，实测数字与已知缺陷见 `examples/2504.09737-README.md`。

悬而未决（细节见 `.agent/notes/002`）：

1. 用户这次审的会议或期刊名称仍未知，合规档位因此定不了。这是第 0 步，定不了就什么都不能动。
2. 评审语言未知。若要写英文意见，本机没有去 AI 味工具，只能靠 `references/de-ai-checklist.md` 的手动清单兜底。
3. **skill 从未在真实稿件上跑过。** 全部验证都在公开论文上做的，真实投稿的排版、页限、参考文献格式都可能把脚本打穿。第一次实战要留出返工时间，并把踩到的坑写回 `examples/` 与 notes。
4. `check_refs.py` 的默认最小化模式对不写 DOI 的会议稿几乎无效，这是已知的性价比缺口，不要为了补它擅自开 `--title-search`。
